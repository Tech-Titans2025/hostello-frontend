import "../index.css"

const Footer = ()  =>{
    return(
        <footer className="footer">
            <a href="#">About us</a>
            <a href="#">Contact us</a>
            <a href="#">Help</a>
            <span>Hostello©</span>
        </footer>
    );
};
export default Footer